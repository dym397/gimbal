# DECISIONS.md

## 2026-07-31
### 决策：视觉关联采用按摄像头补偿后的Y轴代价
**为什么**
- 16:00–17:00现场日志表明，不同固定摄像头来源的SORT投影与真实YOLO中心存在不同但相对稳定的垂直偏差；水平偏差更可能受云台方位零点与相机光轴偏差影响，不适合在当前未重新标定的条件下作为主要匹配依据。
- 单目标必须能够直接绑定；多目标先利用Y轴位置差区分，Y位置非常接近的歧义场景本轮明确不处理，以保持逻辑简单且便于调试。

**实现**
- 先按SORT轨迹最近来源 `logic_id` 修正其投影Y：`logic_id=12` 加 `46.5px`，`logic_id=13` 加 `306.5px`，其他来源加 `0px`。
- 以 `abs(detection_y - compensated_sort_y)` 构造代价矩阵，继续使用匈牙利算法做一对一全局分配，不设置硬拒绝门限。
- X轴不参与代价；日志保留原始 `dx/dy`、二维欧氏误差和实际Y轴代价，便于后续逐摄像头测试并更新补偿量。

**边界**
- 近Y目标可能发生错绑，当前不增加歧义拒绝、历史身份保持或回退状态机。
- 不修改MLP/GRU测距、RID四点匹配与优先裁决、云台控制、安全门控和外部协议。

---

## 2026-07-27
### 决策：匹配复盘使用两份专用结构化日志
**实现**
- 视觉关联按帧记录全部SORT投影、全部检测和完整候选代价矩阵，不只记录最终匹配数量。
- 距离裁决按轨迹记录RID与视觉两路输入以及最终选择，只在测量或决策变化时写入。
- RID既有原始数据和四点候选日志保持不变；通用事件不再重复保存视觉链路的大量字段。

**原因与边界**
- 专用CSV可以用 `vision_frame_ts`、`track_id`、`simple_id` 和RID测量序号连接整条链路，同时避免通用事件字段膨胀和重复行。
- 本次只修改诊断记录，不改变检测、测距、匈牙利分配、RID优先裁决、云台控制或外部协议。
- 视觉结果默认TTL调整为3秒；该参数变化独立于日志结构。

---

## 2026-07-26
### 决策：SORT统一裁决并采用RID优先、视觉补充
**为什么**
- RID和云台视觉来自不同硬件，可以独立、并行地产生距离；不需要让RID匹配成败控制视觉测距是否运行。
- SORT是已经通过二次过滤并取得UI身份的唯一轨迹主线，最终距离必须回到SORT轨迹上，避免出现第二套身份和生命周期。
- 单目标时不应因投影偏差失去距离，多目标时则需要保证一对一，因此视觉关联采用无硬门限的全局分配。

**实现**
- RID四点匹配和视觉测距都只使用 `ui_tracks`，各自输出候选而不直接写最终距离。
- 主线程逐轨迹裁决：6秒内有效RID优先；否则只允许当前云台目标采用新鲜视觉结果；否则输出 `NaN`。
- 视觉检测中心与SORT投影点使用匈牙利算法一对一分配且不设最大像素门限；当前具体代价已由2026-07-31决策更新为“按摄像头补偿后的Y轴绝对残差”。
- RID四点配对样本保留30秒并逐项淘汰。RID与视觉互切时重置距离、径向速度和协方差，再由新来源开始滤波。

**边界**
- 无额外歧义拒绝、临时保底身份或复杂回退状态机；多目标绑定质量通过现场像素误差和ID一致性验证。
- 视觉只补充当前云台目标，不能给未被云台跟随的其他SORT轨迹提供旧视觉距离。
- 不改变UI/UDP协议、云台控制、MLP/GRU算法或打击安全门控；真实硬件联调另行执行。

---

## 2026-07-24
### 决策：SORT–RID采用四点方位残差的偏差/形状双门控
**为什么**
- theta标定误差与设备实际朝向误差当前不能修改，SORT与绝对RID方位之间存在稳定综合偏差，不能要求当前方位角直接重合。
- 仅比较单点方位会把稳定偏差误判为不匹配，也无法利用两条轨迹的相似运动趋势。
- 俯仰偏差明显受目标距离影响，因此本版身份匹配只使用方位轨迹。

**实现**
- 从当前 `ui_tracks/ui_ids` 构造SORT候选。
- SORT方位按RID渲染时间同步，每个不同 `rid_measurement_seq` 只采一个残差点。
- 固定4点窗口计算中位偏差 `bij`、`Ebias` 和 `Eshape95`。
- 默认参数：`b0=3.1198935°`、`Ebias<=2.0°`、`Eshape95<=2.5°`；形状门限从2.0°放宽，用于降低时间采样抖动造成的漏匹配。
- 先硬门控，再用 `Ebias+Eshape95` 作为匈牙利分配代价；没有合格候选就不绑定。

**边界**
- 当前不处理两架飞机交叉时的身份保持/切换问题。
- 匹配成功只决定“哪个RID距离属于哪个SORT”；UI ID、方位、俯仰和相机来源仍全部来自SORT。
- 四点预热、门控拒绝或无RID时仍发送符合UI条件的SORT目标，但距离为 `NaN`；匹配失败不应让视觉目标从UI消失。
- UI状态包的威胁评分只由本包距离分档生成：`<100m` 为100分，`100m~300m` 为50分，`>300m` 为0分；距离无效时评分同样为 `NaN`。
- 不修改theta、`DEVICE_HEADING_DEG`、UI UDP格式、SORT控制逻辑、云台线程或打击安全条件。

---

## 2026-07-13
### 决策：首帧采用 MLP 有效距离，25 帧后切换 GRU 稳距
**为什么**
- 如果把 25 帧 GRU 窗口填满作为首次有效距离的必要条件，首次测距等待时间过长，不利于现场快速确认云台画面和测距链路。
- 物理距离、运动门控和 MLP 在单帧上已经能够给出可用的初始估计；GRU 的职责调整为窗口填满后的时序稳距增强。

**实现语义**
- 第一帧有效检测即可返回 `distance_valid=True`，来源为 `mlp_warmup`。
- `warmup_count=1~24` 表示 GRU 尚未接管，不表示该帧距离无效。
- `warmup_count=25` 后使用滚动 25 帧序列，来源切换为 `gimbal_yolo_gru`；GRU 输出异常时仍按原有规则退回卡尔曼距离。
- 物理测距无效、运动门控拒绝或距离非有限/非正时继续返回无效。

**边界与验证**
- 首帧 MLP 是否有效与“该距离应写给哪条 SORT 轨迹”是两个独立问题；放宽首次输出不能替代 SORT 关联门控和身份一致性验证。
- 日志应同时记录并检查 `distance_source`、`warmup_count`、`distance_valid` 和 `association_error_px`。
- 正式多目标测试需分别统计首帧 MLP 的误差、25 帧后 GRU 的稳定性以及距离到 SORT ID 的绑定正确率。

---

## 2026-07-04
### 决策：云台 YOLO 多目标结果通过统一角度投影绑定 SORT 轨迹
**为什么**
- 固定摄像头与云台摄像头位置、画面和 bbox 坐标不同，不能直接比较 bbox，也不能按 YOLO 返回顺序或置信度绑定目标。
- SORT 已在设备相对方位角/俯仰角坐标系中维护轨迹，因此可以根据当前云台角度和 FOV 将每条轨迹投影到云台 2K 画面。
- YOLO 框中心和 SORT 投影点处于同一云台像素坐标后，才能执行有物理意义的一对一关联。

**实现**
- 对当前全部有效 SORT 轨迹生成云台像素预测点。
- 使用一个或多个原始分辨率 `640x640` 动态 ROI 覆盖预测点，YOLO 强制在 CPU 上运行。
- 将重复 ROI 检测框去重后构造轨迹/检测代价矩阵，并使用匈牙利算法匹配。
- 每个 `track_id` 独立持有运动门控和 25 帧 GRU 序列；仅模型参数共享。
- 匹配必须同时通过最大像素距离、双向最近邻和候选差值门限。
- 对 `ASSOCIATION_AMBIGUOUS`、超门限、漏检、模糊帧均不更新距离序列。

**安全取舍**
- 在没有外观 ReID 的情况下，相同类别目标发生遮挡或交叉时无法从 bbox 数学上保证身份；系统选择暂时不测距，而不是冒险把距离写入错误 ID。
- 云台只服从 master，其他匹配目标只积累距离，不产生云台指令。
- UI 外部协议不变，仍只给 master 发送距离；非 master 的内部距离缓存用于后续切换时缩短预热。

**硬件影响与验证**
- 云台不再持续追求目标严格居中，只有 master 离开安全区域时才重新指向。
- 云台转动、到位驻留不足或画面模糊时不会执行 YOLO，因此测距更新会暂时中断。
- 现场必须验证固定摄像头角度到云台光轴的标定误差。误差过大会导致拒绝匹配；门限过大会增加错绑风险。
- 使用两个真实目标交叉测试，期望交叉期间出现 `ASSOCIATION_AMBIGUOUS` 且两条距离序列均不被错误更新。

---

## 2026-05-30
### 决策：UI 状态包方位角改为地图绝对方位角
**为什么**
- UI 的 `0°` 语义是正北，当前追踪端解算出的方位角是相对设备自身正方向的角度。
- 当设备正方向不是正北时，直接发送相对角会导致 UI 地图上目标方向错误。例如设备正方向朝正南时，相对角 `20°` 实际应显示为地图 `200°`。

**影响**
- 新增配置 `DEVICE_HEADING_DEG`，表示设备自身 `0°` 方向对应的地图方位。
- UI 发送前执行：`map_az = (relative_az + DEVICE_HEADING_DEG) % 360.0`。
- `send_status()` 发送的 `azimuth` 使用 `map_az`。
- 追踪、卡尔曼、云台控制、目标选择仍使用设备自身坐标系方位角，不改变控制闭环。
- 结构化日志 `UI_STATUS_SEND` 新增 `map_az`，原 `pred_az` 继续表示设备相对角。

**验证**
- 公式测试：`relative_to_map_azimuth(20, 180) == 200`。
- 代码语法通过 `compile(..., 'exec')` 检查。

---

## 2026-04-22
### 决策：GPS 启动定位发送一次，超时后使用默认经纬度兜底
**为什么**
- UI 只需要系统启动时获得设备经纬度，不需要 GPS 线程长期持续发送。
- UM980 冷启动或室内环境可能长时间无有效定位；如果一直等待真实 fix，UI 端会迟迟拿不到设备位置。
- 设备部署点通常是固定的，因此 `gps.py` 中的默认经纬度可以作为硬件异常、无卫星或天线问题时的可靠兜底。

**影响**
- `main_tracking_v9.py` 启动后创建 `gps_sender_thread`。
- GPS 线程最多等待 `GPS_FIX_TIMEOUT_SECONDS = 60` 秒获取真实 GGA 定位。
- 若获取成功，发送真实经纬度给 UI。
- 若超时、无卫星、无有效 GGA、串口异常或打开失败，发送 `gps.py` 中的 `DEFAULT_LATITUDE` / `DEFAULT_LONGITUDE`。
- GPS 线程在发送一次真实或默认经纬度后退出。
- 新增 UI GPS 包格式：`0x03 + float(latitude) + float(longitude)`，实现为 `struct.pack('!Bff', 0x03, latitude, longitude)`。

**验证**
- `gps.py` 可直接运行调试 UM980 NMEA 输出。
- `udp_ui_receiver.py` 可模拟 UI 端监听 UDP `9999`，并解析 `0x03` GPS 包。
- 本地已验证 `0x03` 包长度为 9 字节，首字节为 `3`，后续为纬度和经度两个 float。

---

## 2026-04-22
### 决策：GPS 串口纳入现有跨平台串口配置体系
**为什么**
- 项目已有云台、激光、IMU 的平台默认串口和环境变量覆盖机制，GPS 应复用同一配置风格，避免硬编码散落在业务逻辑里。
- Linux 下 `/dev/ttyUSB*` 会受 USB 插拔顺序影响，必须允许通过环境变量覆盖到稳定设备节点。

**影响**
- 新增 `GPS_PORT = _serial_port("GPS_PORT", "gps")`。
- 当前默认值：
  - Windows: `COM8`
  - Linux: `/dev/ttyUSB2`
- `GPS_PORT` 可通过环境变量覆盖；后续部署建议改用 `/dev/serial/by-id/...`。
- `gps.py` 也支持读取 `GPS_PORT` 环境变量，保持单独调试脚本和主程序一致。

**验证**
- `main_tracking_v9.py` 导入后可正确解析 GPS 默认端口。
- Linux 日志中已能看到 GPS 线程按配置端口启动，例如 `waiting for one valid fix on /dev/ttyUSB2@115200`。

---

## 2026-04-16
### 决策：云台抢占改为“同目标按轴更新，切目标整条替换”
**为什么**
- 旧逻辑即使已经拆成 `AZ_PREEMPT_DEG` / `EL_PREEMPT_DEG`，一旦任一轴触发抢占，仍会把整条 `Az/El` 命令一起替换。
- 这会导致低方位阈值在同一目标连续跟踪时“挟持”俯仰轴，使俯仰自己的抢占阈值失去意义。
- 但若目标身份发生切换，又不能只替换单轴，否则会出现“新方位 + 旧俯仰”的混合指向。

**影响**
- 当前抢占阈值正式拆分为：
  - `AZ_PREEMPT_DEG = 0.5`
  - `EL_PREEMPT_DEG = 0.8`
- 控制线程更新规则变为：
  1. `new_track_id != active_track_id` 时，整条替换 `Az/El`
  2. `new_track_id == active_track_id` 时，按轴判断并只更新满足阈值的轴
- 抢占日志新增：
  - `mode=track_switch`
  - `mode=axis_update`
  - `axes=Az/El/Az+El`

**验证**
- 代码层面已完成修改并通过语法检查。
- 这次决策的核心验证目标不是“抢占次数变多或变少”，而是：
  - 同目标连续跟踪时，方位误差不再因为整条命令跳变而周期性重新放大
  - 目标切换时，不出现混合指向
- 最新实机回归结果尚待补充到本条目下。

---

## 2026-04-14
### 决策：室内出现“持续无有效激光距离”时，优先检查目标条件而不是立即怀疑主流程故障
**为什么**
- 在 Linux 接收端 `10.72.2.28:8888` 的重复 `linear` 联调中，`main_tracking_v9.py` 已明确打印 `[Init] Real laser enabled on /dev/ttyUSB1`、`[LaserThread]` 和 `[Laser] start_measurement mode=continuous`，说明激光初始化与后台线程都已启动。
- 但 `logs/main_tracking_v9_20260414_161730.log` 中，尽管每次 `GimbalSettled` 后都尝试取激光缓存，整场测试仍只有 `[Laser] No valid laser distance, use mono distance`。
- 随后仅将激光指向更远目标，不改代码、不改串口配置，`logs/main_tracking_v9_20260414_162745.log` 就恢复了连续真实测距，说明问题主要受目标距离/反射条件影响。

**影响**
- 激光联调排障顺序调整为：
  1. 先确认激光线程、连续测量命令和 settled 触发都存在
  2. 再检查目标距离、材质、反射面角度与瞄准方向
  3. 最后才怀疑主流程代码或串口协议回归
- 保持当前“连续测量 + settled 时消费最新缓存值”的结构不变。
- 保持当前“无有效激光值时退回 mono distance”的降级策略不变。

**验证**
- `logs/main_tracking_v9_20260414_161730.log`：
  - 激光线程已启动，连续测量命令已发送
  - 整场测试中 settled 后均退回 mono distance
- `logs/main_tracking_v9_20260414_162745.log`：
  - 在更远目标条件下恢复 `GimbalSettled=22`
  - `GimbalTimeout=0`
  - `Laser Triggered=22`
  - `Laser No valid=0`
  - 真实激光距离约 `6.2m ~ 7.9m`

---

## 2026-04-14
### 决策：真实激光接入主流程，并保留 `USE_MOCK_LASER` 作为统一切换开关
**为什么**
- 原先主程序虽然有 `LASER_PORT`、`SharedHardwareState.raw_laser_dist` 和 `sddm_laser.py`，但没有任何后台线程把真实激光数据写入共享状态，导致主流程实际一直退回 mono distance。
- 调试阶段既需要真实激光联调，也需要在无激光硬件时保留可重复的模拟输入，因此需要像云台一样提供统一开关。

**影响**
- `main_tracking_v9.py` 新增 `USE_MOCK_LASER`
- `USE_MOCK_LASER=False` 时：
  - 启动真实 `SDDMLaser`
  - 后台线程发送连续测量命令
  - 持续读取并缓存真实激光值
- `USE_MOCK_LASER=True` 时：
  - 不打开真实激光串口
  - 使用当前主目标的新鲜 mono 距离模拟激光输入

**为什么选择连续测量**
- 当前控制结构是在 `GimbalSettled` 后消费“最新激光缓存值”，而不是在 settled 当下发单次测量并阻塞等待。
- 连续测量更适合现有非阻塞串口/控制线程结构，也更容易保持日志与目标绑定关系稳定。

**验证**
- 短时测试中曾出现“仅无效帧”的情况，主流程正确退回 mono distance。
- 长时 `linear` 场景测试中，真实云台 `COM3` + 真实激光 `COM8` 已验证：
  - `GimbalSettled=19`
  - `GimbalTimeout=0`
  - `Laser Triggered=19`
  - `Laser No valid=0`
- 真实激光返回的是实物环境距离 `4.7m ~ 6.5m`，说明真实激光链路已生效；发送脚本中的 `320m` 仅作用于视觉输入。

---

## 2026-04-14
### 决策：按手册补强 SDDM 激光驱动帧校验，但保持驱动接口不变
**为什么**
- 原始驱动的起停测量命令与 CRC 思路基本正确，但数据读取时只做 CRC 校验，不足以区分“CRC 恰好正确但不是目标数据帧”的情况。
- 该项目是硬件耦合系统，驱动接口应稳定，优先补强校验而不是改写上层调用方式。

**影响**
- `sddm_laser.py` 现在额外校验：
  - 数据帧 `MsgCode=0x03`
  - `PayloadLen=0x04`
- 无效标志位不再返回负距离，统一按“无有效测距”处理
- 保持 `start_measurement()`、`stop_measurement()`、`read_distance()` 接口不变

**验证**
- `COM8` 原始串口抓包曾出现连续的 `fb0300040000000002`，被正确识别为“有回包但无有效距离”
- 后续长测中已出现持续有效测距，说明校验补强没有破坏正常数据解析

## 本文件用途
记录已经形成共识的工程决策、原因、影响和验证方法。这里的内容用于避免后来者重复推翻已验证的方向；若后续测试推翻某个参数，应在对应日期下追加修订说明，而不是直接删除历史原因。

## 2026-04-10
### 决策：将串口与运行环境配置调整为双平台兼容
**为什么**：当前开发调试在 Windows 上进行，但部署目标仍可能是 Linux，需要让同一份代码和文档同时支持两端运行，而不拆成两套主逻辑。
**影响**：
- 文档中的运行环境描述改为双平台兼容。
- 主程序串口默认值按平台切换：Windows 使用 `COM3` / `COM4` / `COM5`，Linux 使用 `/dev/ttyUSB0` / `/dev/ttyUSB1` / `/dev/ttyUSB2`。
- 串口校验逻辑按平台切换：Windows 校验 `COMx`，Linux 校验 `/dev/...`。
**待验证**：
- 在 Windows 与 Linux 两端分别确认 GT06Z、激光、IMU 的实际串口设备名。
- 使用真实或 mock 云台启动 `main_tracking_v9.py`，确认双平台下串口初始化、控制线程启动和 UDP 收发不受影响。

---

## 2026-04-10
### 决策：使卡尔曼滤波器时间步长 (dt) 严格依赖实际帧率
**为什么**：测试表明，硬编码的 `MIN_DT=0.08` 与实际的 15 FPS (~0.066s) 不匹配。这会导致卡尔曼滤波器系统性地低估目标的实际角速度，从而使前馈补偿过于保守，云台跟随滞后。
**影响**：
- 废弃固定的时间步长下限。
- 卡尔曼预测必须基于两次更新之间的实际时间差或帧率期望。
**已实施**：
- `main_tracking_v9.py` 中动态参数返回的 `MIN_DT` 已改为 `0.001`。
- `StandardKalmanTrack` 默认 `self.min_dt` 已改为 `0.001`。
- 该值只用于防止异常极小 `dt`，不再把 15FPS 场景的 `~0.066s` 抬高到 `0.08s`。
**待验证**：
- 复测 `udp_sender_tracking_scenarios.py` 的 `linear` 场景：320m、5.1m/s、15FPS、`cx=700 -> 3686.67`、`cy=1080`。
- 目标角速度估算应从旧日志中的约 `0.76°/s` 更接近按观测角度反推的 `~0.91°/s`。

---

## 2026-04-10
### 决策：解耦方位轴 (Pan) 与俯仰轴 (Pitch) 的动态控制参数
**为什么**：实测发现俯仰轴的机械响应显著慢于方位轴（从 -9° 爬升至 28.5° 需要超过 2s）。使用单一全局的 `PREDICT_DELAY=0.25` 过于粗糙，无法同时兼顾两个轴的不同物理迟滞。
**影响**：
- 拆分提前量控制，分别实现 `AZ_PREDICT_DELAY` 和 `EL_PREDICT_DELAY`。
- 未来的控制逻辑修改必须尊重多轴动力学的不对称性。
**已实施的临时缓解**：
- 当前尚未拆分 `PREDICT_DELAY`，仍保留单一 `PREDICT_DELAY=0.25`。
- 已新增启动位姿初始化：系统启动后先通过 `gimbal_cmd_queue` 投递 `cmd_id=0, track_id=-1` 的初始化命令，目标姿态为 `Az=GIMBAL_AZ_BASE`、`El=0.0°`。
- 该初始化命令仍由 `gimbal_control_thread` 统一下发，保持云台串口单一控制线程所有权。
**待验证**：
- 上电前确认 `Az=GIMBAL_AZ_BASE`、`El=0.0°` 方向安全无遮挡，不会撞限位。
- 复测首个目标出现时俯仰轴是否不再从约 `-9°` 大幅追赶到 `28.5°`。

---

## 2026-04-10
### 决策：分离“首次捕获 (Initial Acquisition)”与“稳定跟踪 (Stable Tracking)”状态
**为什么**：单一参数无法兼顾全周期。目标刚进入视野时需要激进的参数以实现“快速拉近”；一旦进入视场中心区域，则需要平滑的参数来“抑制抖动”。
**影响**：
- 在主控逻辑中引入阶段标志 (Phase State Machine)。
- 针对捕获阶段和稳定阶段，应用不同的前馈权重、卡尔曼增益或限速策略。

---

## 2026-04-10
### 决策：强制拉开“抢占阈值 (Preempt)”与“到位阈值 (Settle)”的迟滞区间 (Hysteresis)
**为什么**：当 `PREEMPT=0.7` 和 `SETTLE_THRESHOLD=0.8` 过于接近时，云台极易在“即将到位”和“被新微小误差打断”的边缘状态下反复横跳，导致严重的机械震荡。
**影响**：
- 必须确立严格的迟滞控制原则，例如 `PREEMPT_DEG` 应显著大于 `SETTLE_THRESHOLD`。
- 在状态到位之前，必须过滤掉落在死区内的微小位置更新指令。
**已实施**：
- `GIMBAL_PREEMPT_DEG` 已从 `0.7` 调整为 `1.5`。
- `GIMBAL_SETTLE_THRESHOLD` 已从 `0.8` 先收紧到 `0.5`，随后根据云台约 `0.1°` 精度进一步收紧到 `0.3`。
**待验证**：
- 观察旧日志中 `delta=0.71~0.74°` 的抢占是否消失或明显减少。
- 观察更严格 `0.3°` 到位阈值是否导致 `GimbalTimeout` 增多；若超时明显增多，再考虑在 `0.3~0.5°` 区间回调。

---

*(保留以下历史决策)*

## 2026-04-09
### 决策：保持 `main_tracking_v9.py` 作为主要的集成入口
... (略，保持原有内容)
### 历史决策：曾保持单一部署平台作为主要运行环境（已被 2026-04-10 双平台兼容决策取代）
... (略，保留原有历史内容；当前执行以 2026-04-10 双平台兼容决策为准)
### 决策：保留云台控制的高层适配器与底层驱动的分离架构
... (略，保持原有内容)
### 决策：通过专用线程保持云台控制的“单一所有者”模型
... (略，保持原有内容)
### 决策：保留 UI 角度与云台控制角度的区分
... (略，保持原有内容)
### 决策：将运行时日志作为一等的调试工具
... (略，保持原有内容)
### 决策：使用项目文件作为长期的上下文记忆
... (略，保持原有内容)
### 决策：使用“最新有效状态”进行传感器融合，而非严格的时间戳对齐
... (略，保持原有内容)
### 决策：在极坐标/角度空间中追踪目标，而非笛卡尔空间 (XYZ)
... (略，保持原有内容)
