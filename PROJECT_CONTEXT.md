# PROJECT_CONTEXT.md

## 2026-07-12 云台控制可靠性修复
- 现场诊断日志新增 `GIMBAL_VISION_DETECTION`：对每个新云台相机帧的全部YOLO目标，在SORT关联和距离有效性判断前记录 bbox中心及其相对2K画面中心的 `dx/dy px`、归一化偏差和角度偏差，便于直接判断光轴/目标偏差。
- 夜间 `640×480` 坐标已用 `20260711_222934` 的 3135 条 measurement 全量复算：当前以 `(320,240)` 归一化的结果与恢复到 `2560×1440` 后解算完全等价，日志与当前公式最大误差仅 `5e-7°`。
- 同一日志发现 `cmd_id=4/6` 的俯仰已到位，但方位分别停在上一位置并保持 `7.75°/6.57°` 误差直至超时；原有0.9秒强制重发曾在2026-07-07被删除。
- 当前重新加入0.9秒未到位重发，并升级为按轴重发：Az未到位只发送 `0x4B`，El未到位只发送 `0x4D`；驱动返回每轴TX/ACK诊断并写入 `GIMBAL_CMD_RETRY`。
- 已把“到达命令位置”和“物理静止”拆分：`is_settled` 保留原有 `<0.3°` 双轴到位语义和打击安全门控；`is_stationary` 由编码器连续稳定0.35秒判定，供稳定画面上的YOLO/测距使用，避免云台实际静止但预览长期显示 `GIMBAL_MOVING`。
- 现有重定位策略保持为中央60%安全视场、连续3帧越界才动作，不要求把目标持续保持在画面中心。
- 已通过 `py_compile`、GT06Z单轴帧测试及Mock端到端UDP测试：中心/安全区内/水平越界/垂直越界四阶段的角度误差均为0，4条Mock命令全部到位、0次超时；故障注入丢弃首次Az后于0.906秒仅重发Az并最终 settled/stationary。
- 已完成真实GT06Z隔离测试：仅在初始化附近移动（Az约57.4°~63.2°、El约-4.8°~-0.4°），5阶段坐标解算误差为0/舍入 `5e-7°`，初始化与3条目标命令全部到位、0次超时、4次stationary；安全区内移动没有发命令。出现1次仅El重发并最终到位。串口日志显示ACK缺失不等价于命令未执行，因此当前按姿态误差重发策略保持不变。测试结束已回到初始化容差内，服务当前暂停。

## 2026-07-11 最新进展
- 当前 Linux 板端仓库位于 `/home/linaro/gimbal`，SSH 地址为 `linaro@192.168.40.154`；开发机公钥已配置到板端，可以使用 `ssh linaro@192.168.40.154` 免密登录。
- SORT 轨迹安全修复已经落地：UI 的检测来源改为每条轨迹自身最近来源；关联门限增加全局角度硬上限；长时间丢失轨迹使用严格重关联门限；匈牙利匹配前执行不可匹配门控；UI 使用比内部 5 秒轨迹保留更严格的新鲜度门限隐藏旧预测。
- 云台视觉 simple 模式已经改为“先测距，后关联 SORT”，用于优先保证悬停目标能够稳定进入距离模型：
  1. 云台未停稳时不执行 YOLO 和测距，停稳后才处理相机帧。
  2. YOLO 检测画面内全部目标，不再选择画面中心最近的单个目标。
  3. 检测结果先与画面内临时目标 ID 做轻量连续性关联；该关联不使用 SORT，只用于保证多个目标分别进入自己的 25 帧缓冲区。
  4. 每个临时目标持有独立 `_DistanceRuntime`，原有物理距离、MLP、运动门控和静态 25 帧 GRU 实现保持不变。
  5. runtime 更新后输出 `simple_measurements`，其中包含临时 ID、全图 bbox/中心、预热帧数、距离及有效标志。
  6. `main_tracking_v9.py` 再将这些结果与当前有效 SORT 轨迹的云台画面预测位置做匈牙利一对一匹配，并在像素硬门限外禁止回填。
  7. 匹配成功且距离新鲜有效时，通过 `track.set_mono_distance()` 写入对应轨迹，随后 UI 和打击端沿用既有统一轨迹结果。
- 当前流程中，SORT 匹配不再是进入 25 帧距离模型的前置条件。即使某帧无法回填 SORT，临时画面目标的缓冲区仍继续积累，后续帧可再次尝试回填。
- 当前默认 simple 画面关联/回填像素门限为 `180px`，可通过 `GIMBAL_VISION_SIMPLE_ASSOC_MAX_PX` 调整。该门限用于防止距离写错轨迹，不参与决定一帧是否进入距离 runtime。
- 已完成的软件验证：两个模拟目标在不提供 SORT 输入的情况下分别积累到 `25/25`；两个 runtime 共享模型资产但时序状态独立；两个核心文件通过板端 `py_compile`；6 个追踪安全测试通过直接调用。板端未安装 `pytest`。
- 尚未完成真实硬件回归。本轮下一验证重点是：悬停单目标/多目标能否稳定完成 25 帧预热、不同目标缓冲区是否串帧、距离结果与 SORT/UI/打击目标 ID 是否一致、YOLO 间歇漏检后的恢复时间。
- 本轮没有修改距离模型内部、外部 UDP/UI 包格式、云台控制/抢占/到位逻辑或打击安全条件。

## 2026-07-04 历史基线（测距关联流程已被 2026-07-11 版本替代）
- 本节保留用于说明旧实现。下面“先将 YOLO 与 SORT 一对一绑定，再允许进入 25 帧序列”的描述不再代表当前 simple 模式；当前行为以 `2026-07-11 最新进展` 为准。
- 已将 `distance model` 的 YOLO 检测、物理距离、MLP 与 25 帧 GRU 稳距链路移植到 `gimbal_vision_ranging.py`。
- 云台相机设备名默认为 `000000008`，采集分辨率固定为 `2560x1440`；YOLO 强制使用 CPU，不调用 CUDA。
- 云台运动期间只持续清空相机缓存，不执行 YOLO；云台连续到位并经过清晰度检查后才开始测距。
- 云台视觉已由单目标改为多目标：
  1. `main_tracking_v9.py` 将当前全部有效 SORT 轨迹投影到云台 2K 画面。
  2. 视觉线程围绕这些预测位置生成原始分辨率 `640x640` 动态 ROI。
  3. YOLO 检测 ROI 内全部目标，并将框恢复到 2K 全图坐标。
  4. 使用匈牙利算法按预测像素位置进行一对一绑定。
  5. 每个 `track_id` 独立维护运动门控、MLP/GRU 25 帧历史和距离结果。
- 绑定采取保守策略：必须满足像素门限、双向最近邻以及第一/第二候选差值门限；目标靠近或交叉导致身份不明确时返回 `ASSOCIATION_AMBIGUOUS`，该帧不更新任何不确定目标的距离序列。
- 云台仍然只由 `master_track` 控制；非 master 目标可以在画面内被检测和积累距离，但不会驱动云台。
- UI `0x02` 协议未变化：仍发送全部有效轨迹，只有 master 距离字段允许携带新测距离，其余轨迹距离为 `NaN`。
- 打击端仍只发送 master，并要求云台到位、master 框处于安全区域且距离结果新鲜有效。
- 当前 Windows CPU 实测：30 帧完整单 ROI 链路约 `3.5 FPS`，两块 ROI 合批推理单帧约 `0.17s`；实际速度受画面、线程和 CPU 影响，现场应重点核查目标数量、25帧预热时间和结果 TTL。
- Windows 默认相机名为 `000000008`；Linux 部署必须通过 `GIMBAL_CAMERA_SOURCE` 指定实际 `/dev/video*` 或稳定设备路径。
- 文件视频源现支持全链路回放测试：`GIMBAL_VIDEO_START_FRAME` 指定起始帧，`GIMBAL_VIDEO_REALTIME=1` 按视频原始 FPS 播放，`GIMBAL_VIDEO_LOOP` 控制是否循环；这些配置不影响真实摄像头采集。
- 已使用 `20~250米.mkv` 从第600帧开始完成一次真实模型全链路测试：同步发送检测框 UDP，经过 SORT、云台动态 ROI、CPU YOLO、MLP/GRU、UI 和打击协议。视觉达到25帧预热后，UI收到23个有效距离包（约 `26.376m~30.249m`），打击端收到23个同目标ID且校验正确的数据包，主程序无异常。

## 2026-05-30 最新进展
- UI 端目标方位角语义已调整为地图绝对方位角：UI 的 `0°` 固定表示正北。
- 系统内部仍以设备自身正方向为 `0°` 做目标解算、追踪、云台控制；只在发给 UI 前做方位转换。
- 新增配置 `DEVICE_HEADING_DEG`：
  - 含义：设备自身 `0°` 方向对应的地图方位
  - 正北为 `0`
  - 正东为 `90`
  - 正南为 `180`
  - 正西为 `270`
- UI 发送转换公式：`map_az = (relative_az + DEVICE_HEADING_DEG) % 360.0`。
- `UI_STATUS_SEND` 结构化日志新增 `map_az` 字段，`pred_az` 仍保留设备自身坐标系下的追踪角。

## 2026-04-22 最新进展
- GPS 启动定位功能已接入 `main_tracking_v9.py`，用于系统启动时向 UI 发送一次设备经纬度。
- 当前 GPS 数据路径：
  1. `main_tracking_v9.py` 创建 `UISender`
  2. 启动 `gps_sender_thread` 后台线程
  3. 线程调用 `gps.py` 中的 `read_gps_fix()`
  4. `read_gps_fix()` 打开 GPS 串口并解析 UM980 NMEA GGA 数据
  5. 若 60 秒内拿到有效定位，向 UI 发送真实经纬度
  6. 若 60 秒内仍无有效定位，向 UI 发送 `gps.py` 默认经纬度
  7. 发送一次后 GPS 线程退出
- GPS UI 协议为独立包，不复用目标状态包：
  - `0x03 + float(latitude) + float(longitude)`
  - 代码实现为 `struct.pack('!Bff', 0x03, latitude, longitude)`
  - 现有目标状态包仍为 `0x02`，格式不变
- 当前 GPS 串口配置与其他硬件统一：
  - `GPS_PORT = _serial_port("GPS_PORT", "gps")`
  - Windows 默认 `COM8`
  - Linux 当前默认 `/dev/ttyUSB2`
  - 可通过 `GPS_PORT` 环境变量覆盖；部署时建议优先使用 `/dev/serial/by-id/...` 避免 USB 插拔顺序变化
- `gps.py` 的 `DEFAULT_LONGITUDE` / `DEFAULT_LATITUDE` 现在具有运行兜底意义：GPS 模块无信号、无卫星、串口异常或超时后，会用该默认坐标发给 UI。部署前应改成设备实际安装点坐标。
- 已新增 `udp_ui_receiver.py`，可在 UI 端或调试电脑监听 UDP `9999`，解析 `0x02` 状态包和 `0x03` GPS 包，用于验证 Linux 端发包。

## 2026-04-16 最新进展
- 云台抢占控制已进一步解耦为“同目标按轴更新、切目标整条替换”。
- 当前方位/俯仰抢占基线为：
  - `AZ_PREEMPT_DEG = 0.5`
  - `EL_PREEMPT_DEG = 0.8`
- 当前控制线程行为：
  1. 若 `new_track_id != active_track_id`，视为目标切换，`Az/El` 整条命令一起替换。
  2. 若仍是同一 `track_id`，则分别比较 `dAz` 与 `dEl`：
     - `dAz > AZ_PREEMPT_DEG` 时只更新方位目标
     - `dEl > EL_PREEMPT_DEG` 时只更新俯仰目标
     - 两轴都满足则同时更新
- 这样做的目的，是避免在同一目标连续跟踪时，低方位阈值把俯仰目标也一起频繁刷新；同时又避免在切换到新目标时出现“新方位 + 旧俯仰”的混合指向。
- 当前这次代码变更后，尚未写入新的实机回归结果；后续应优先复测 `linear` 场景，验证方位误差是否不再呈现明显的周期性放大。

## 2026-04-14 最新进展
- 真实激光已正式接入主流程，不再只是保留 `LASER_PORT`、`raw_laser_dist` 和驱动文件但没有数据源。
- 当前激光数据路径：
  1. `main_tracking_v9.py` 在 `USE_MOCK_LASER=False` 时实例化 `SDDMLaser`
  2. 启动后台线程发送连续测量命令
  3. 后台线程循环读取真实串口数据并更新 `SharedHardwareState.raw_laser_dist`
  4. 云台 `GimbalSettled` 后读取最新激光缓存，并绑定到当前 `laser_track_id`
- 当前保留两种激光模式：
  - 真实激光：`USE_MOCK_LASER=False`
  - 模拟激光：`USE_MOCK_LASER=True`，使用当前主目标的新鲜 mono 距离模拟激光输入
- `sddm_laser.py` 已根据手册确认协议正确，当前实现使用：
  - 串口 `115200 / 8N1`
  - 连续测量命令 `MeaType=1, MeaTimes=0`
  - 数据帧头 `0xFB`
  - 距离单位 `dm -> m`
  - 附加校验：`MsgCode=0x03`、`PayloadLen=0x04`
- 最新实机验证结果：
  - 真实云台 `COM3` 与真实激光 `COM8` 已可同时启动
  - 15 秒 `linear` 场景下，云台 `GimbalSettled=19`、`GimbalTimeout=0`
  - 同场景下，真实激光 `Laser Triggered=19`、`Laser No valid=0`
  - 激光测到的是现实场景距离 `4.7m ~ 6.5m`，说明代码已使用真实激光；发送脚本中的 `320m` 只代表视觉输入，不代表激光会返回 320m
- 2026-04-14 Linux 追加联调结果：
  - 发送端对 `10.72.2.28:8888` 反复执行 `15FPS / 15s / linear / cx=700 -> 3686.67 / cy=1080 / mono=320m`
  - `logs/main_tracking_v9_20260414_161730.log` 中，激光线程和连续测量均正常启动，但所有 settled 均退回 mono distance，说明“主流程未拿到有效激光值”可以在室内近距离/弱反射目标条件下出现
  - `logs/main_tracking_v9_20260414_162745.log` 中，仅调整激光指向更远目标后即恢复 `Laser Triggered=22`、`Laser No valid=0`，实测距离约 `6.2m ~ 7.9m`
  - 当前可确认：激光链路与主流程绑定逻辑是通的，剩余问题集中在“什么目标条件下会返回有效值”的边界验证

## 本文件用途
给第一次接触项目的人快速建立工程全貌：系统做什么、数据如何流动、哪些模块与硬件耦合、当前控制基线是什么。实现细节和待办请继续阅读 `TODO_NEXT.md` 与 `DECISIONS.md`。

## 项目名称
支持 Windows 调试与 Linux 部署的云台目标追踪与测距系统

## 主入口
- `main_tracking_v9.py`

## 项目目标
接收 UDP 目标检测，进行连续追踪，驱动 GT06Z 云台进行实时跟随，并将数据回传。针对真实物理延迟进行闭环调优。

## 当前进度摘要
- 主流程已在 `main_tracking_v9.py` 集成：UDP 输入、目标归一化、Kalman 追踪、目标选择、云台控制、UI 回传。
- 云台视觉测距已支持停稳后对画面全部 YOLO 目标建立独立 25 帧缓冲，先完成距离 runtime 更新，再将结果回填到 SORT；测距积累不再受 SORT 前置匹配成败影响。
- UI/打击端继续消费 SORT 统一轨迹结果，外部协议不变；有效视觉距离由测距后关联写入轨迹，不再使用中心目标专用旁路。
- 多相机/SORT 安全修复已覆盖轨迹自身检测来源、关联硬上限、长丢失严格重关联、匈牙利前门控和 UI 陈旧轨迹隐藏。
- 当前代码已支持双平台串口默认值切换：Windows 默认 `COM3` / `COM4` / `COM5`，Linux 默认 `/dev/ttyUSB0` / `/dev/ttyUSB1` / `/dev/ttyUSB2`；实际运行时仍可通过 `GIMBAL_PORT` / `LASER_PORT` / `IMU_PORT` 覆盖。
- 当前调优依据来自 `logs/main_tracking_v9_20260409_154819.log`：15FPS、320m、5.1m/s、水平匀速直线运动，`cx=700 -> 3686.67`，`cy=1080`。
- 已完成第一轮控制修正并在 2026-04-16 继续演进：Kalman `MIN_DT=0.001`、抢占阈值改为 `AZ_PREEMPT_DEG=0.5` / `EL_PREEMPT_DEG=0.8`、`GIMBAL_SETTLE_THRESHOLD=0.3`、启动时云台归位到 `Az=GIMBAL_AZ_BASE` / `El=0.0°`。
- 尚未完成：新测距流程的真实悬停目标端到端回归、`PREDICT_DELAY` 双轴拆分、首次捕获/稳定跟踪的独立参数状态机、激光有效工作距离/反射条件边界验证。

---

## 端到端管线 (End-to-end pipeline)

### 1. 上游输入与检测归一化
接收板卡/相机/BBox/距离数据，并通过 `parse_udp_objects` 进行归一化。

### 2. 目标追踪 (Kalman Tracking with Dynamic DT)
系统包含基于卡尔曼滤波器的追踪器 (`StandardKalmanTrack`)。
- **状态量**：方位角、俯仰角、方位角速度、俯仰角速度。
- **自适应时间步长**：追踪器的预测步长 `dt` 必须基于实际的视频帧率（如 15FPS 约等于 0.066s）动态计算，避免系统性低估目标速度。

### 3. 双阶段追踪控制模型 (Dual-Phase Tracking Model)
为了平衡响应速度与防抖，系统区分了两种控制阶段：
- **首次捕获模式 (Initial Acquisition)**：目标刚出现或刚切换时，采用激进的参数实现“快速拉近”。
- **稳定跟踪模式 (Stable Tracking)**：目标处于视野中心区域后，采用平滑的参数来“抑制抖动”。

### 4. 解耦的云台控制与前馈补偿 (Decoupled Gimbal Control & Feedforward)
- **多轴独立预测**：由于俯仰轴 (Pitch) 的机械响应往往慢于方位轴 (Pan)，`PREDICT_DELAY` 必须双轴解耦（例如 `AZ_PREDICT_DELAY` 和 `EL_PREDICT_DELAY`）。
- **抗震荡迟滞 (Anti-thrashing Hysteresis)**：指令抢占阈值 (`PREEMPT_DEG`) 与到位阈值 (`SETTLE_THRESHOLD`) 必须保持合理的间距，避免云台在“快到位但又被新命令刷新”的边缘状态下反复横跳。
- **初始位姿修正**：系统需处理云台初始位姿过远（如俯仰 -9° 到 28.5° 需要 >2s）带来的初次锁定迟缓问题。

### 4.1 当前控制基线 (2026-04-10)
- `main_tracking_v9.py` 已将 Kalman `MIN_DT` 从固定 `0.08` 调整为 `0.001`，仅作为异常极小 `dt` 保护，避免 15FPS (`~0.066s`) 场景被强行抬高。
- 抢占阈值已在 2026-04-16 更新为 `AZ_PREEMPT_DEG=0.5`、`EL_PREEMPT_DEG=0.8`；`GIMBAL_SETTLE_THRESHOLD=0.3` 保持不变。当前策略是同目标连续跟踪时按轴更新、切换目标时整条替换，并继续利用云台约 `0.1°` 精度收紧到位判定。
- 系统启动后会先通过现有 `gimbal_cmd_queue` 投递初始化姿态命令：方位角 `GIMBAL_AZ_BASE`，俯仰角 `0°`。串口下发仍由 `gimbal_control_thread` 单一控制线程执行。
- 尚未完成 `PREDICT_DELAY` 的双轴拆分；当前仍是单一 `PREDICT_DELAY=0.25`，俯仰轴慢的问题先通过启动位姿初始化进行部分缓解，后续仍需垂直/斜向运动测试后再调 `AZ_PREDICT_DELAY` / `EL_PREDICT_DELAY`。

### 5. 共享状态与传感器融合
通过 `SharedHardwareState`（使用线程锁保护）融合最新有效的单目、激光、IMU 数据，放弃严格时间戳对齐，改为基于 TTL 的最新有效值融合。

### 5.1 云台停稳后的多目标视觉测距（2026-07-11）
当前 simple 模式的数据流为：

```text
云台停稳
-> YOLO 检测全画面目标
-> 画面内临时 ID 一对一连续性关联
-> 每个临时 ID 独立积累 25 帧并调用既有距离 runtime
-> 输出距离、bbox/中心和有效状态
-> 与当前 SORT 画面预测位置做后置一对一关联
-> 有效距离写入对应 SORT 轨迹
-> UI/打击端读取统一轨迹结果
```

- 第一处“画面内临时 ID 关联”只用于区分多个 YOLO 目标的时序缓冲，不读取 SORT 数据，因此不会复现旧版“匹配不上 SORT 就丢帧”的问题。
- 第二处“SORT 后置关联”决定距离写回哪条全局轨迹。超出像素硬门限的组合不可匹配，避免把正确距离写到错误目标。
- 后置关联失败时，该帧仍已进入对应临时目标的距离 runtime；缓冲区继续保留并在后续帧再次尝试回填。
- 当前优化目标是悬停或低速目标。目标快速交叉、严重遮挡或 YOLO 框在不同目标间跳变时，临时画面 ID 仍可能发生身份交换，必须通过真实多目标日志继续验证。

---

## 硬件相关模块 (Hardware-related modules)
- `gimbal_interface.py` & `GT06Z_gimbal.py`: 抽象接口与底层驱动。
- `sddm_laser.py` & `hwt905_driver.py`: 独立串口传感器的后台读取线程。

## 运行环境与架构风格
双平台运行环境。Windows 调试时串口设备使用 `COMx` 命名；Linux 部署时串口设备使用 `/dev/ttyUSB*` / `/dev/ttyACM*` 命名。默认端口已按平台切换，实际运行前仍需按设备管理器或 Linux 设备节点确认。硬件调试导向，强调日志覆盖率、显式线程控制与物理真实性。
