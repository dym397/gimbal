# 云台主动视觉跟踪方向调研总结

## 1. 当前项目与潜在论文方向

当前 `gimbal` 项目是一个面向真实硬件的云台目标追踪与测距系统，已有基础包括：

- 接收上游目标检测/跟踪数据。
- 使用 Kalman 滤波和匈牙利匹配维护多目标轨迹。
- 从多个目标中选择当前主目标。
- 将目标位置转换为云台方位角和俯仰角控制命令。
- 控制 GT06Z 云台跟随目标。
- 在云台稳定后读取激光测距数据。
- 将目标状态和 GPS 等信息通过 UDP 回传 UI。

从论文角度看，这个项目不适合简单描述为“基于 YOLO 的目标跟踪系统”。更准确的定位是：

> 面向嵌入式云台平台的多目标主动视觉跟踪与延迟补偿控制方法。

核心不是重新做一套完整大系统，而是研究：

> 如何把检测/多目标跟踪输出的离散、抖动、延迟目标状态，转换为真实云台可稳定执行的闭环控制目标。

## 2. 几类相关任务的区别

### Detection：目标检测

目标检测只处理单帧图像。

输入：

```text
一帧图像
```

输出：

```text
类别 class + 目标框 bbox + 置信度 confidence
```

典型算法包括 YOLO、Faster R-CNN、DETR、RT-DETR 等。

检测不关心跨帧 ID，也不关心云台如何运动。

### SOT：单目标跟踪

SOT 关注一个指定目标。

问题是：

```text
第一帧告诉算法要跟哪个目标，后续每一帧找出这个目标的位置。
```

典型算法包括 SiamFC、SiamRPN++、DiMP、STARK、OSTrack、MixFormer、SeqTrack 等。

SOT 主要解决单个目标在遮挡、尺度变化、运动模糊下的持续定位问题。

### MOT：多目标跟踪

MOT 关注多个目标，并且要为每个目标维护稳定 ID。

输入通常是检测器每帧输出的目标框。

输出：

```text
track_id + bbox + confidence
```

典型算法路线：

```text
SORT = Kalman + 匈牙利匹配
DeepSORT = SORT + ReID 外观特征
ByteTrack = 利用高低置信度检测框进行关联
OC-SORT = 强化运动观测和轨迹关联
BoT-SORT / StrongSORT = 结合运动、外观和相机运动补偿
```

当前项目中的 Kalman 滤波和匈牙利匹配，已经属于 SORT/DeepSORT 风格的 tracking-by-detection 框架。

### Active Tracking：主动跟踪

Active Tracking 不是只在视频里画框，而是控制相机、云台、机器人或无人机，让目标保持在视野中心。

普通 MOT 解决：

```text
目标是谁，目标在哪里。
```

Active Tracking 解决：

```text
云台该怎么动，才能持续稳定地对准目标。
```

这正是当前项目最有差异化的部分。

### Embedded Tracking：嵌入式跟踪

Embedded Tracking 不是单独一种跟踪任务，而是部署约束。

它关注：

```text
FPS、端到端延迟、模型大小、NPU/CPU 占用、功耗、量化精度损失。
```

当前项目如果部署到 RK3588，就属于：

```text
Detection + MOT + Active Tracking + Embedded Deployment
```

## 3. 为什么不建议把重点放在 Detection

如果完整链路都作为创新点：

```text
改进 YOLO 检测
+ 多目标跟踪
+ 主目标选择
+ 云台控制
+ RK3588 部署
+ 激光测距
```

这个范围已经接近硕士毕业论文级别，对一篇小论文来说过大。

更合理的策略是：

```text
Detection 作为输入基础
MOT / 主目标状态估计 / 云台主动跟踪控制作为研究重点
```

Detection 可以使用成熟 YOLO、Ultralytics YOLO track、ByteTrack/BoT-SORT 后端，或者使用组内师兄的 DNW-YOLO 作为检测前端之一。

不建议重新主打“改进 YOLO”，原因是：

- 小目标检测方向很卷。
- 组内师兄已经有一篇类似赛道论文。
- 如果继续做 YOLO 结构改进，容易变成同质化工作。
- 你的平台优势在云台闭环控制，而不是单帧检测。

## 4. 师兄 DNW-YOLO 论文的定位

师兄最近的文章：

```text
DNW-YOLO: a cost-effective hardware-software system for small UAVs in complex backgrounds
Journal of Real-Time Image Processing
```

它的核心方向是：

```text
Detection + Embedded Deployment
```

主要贡献包括：

- 构建多天气小 UAV 数据集 VSUAV。
- 基于 YOLOv8s 改进小目标检测网络。
- 加入 Wave-LLG、HyperSPP、CDetect 等结构。
- 使用知识蒸馏和 PTQ 量化。
- 部署到 K230 平台。
- 在 Det-Fly、VSUAV、VisDrone2019 和 K230 板端上验证。

这篇文章解决的是：

```text
边缘端如何看见复杂背景下的小无人机，并且跑得动。
```

而当前项目可以进一步解决：

```text
边缘端看见目标以后，如何让云台稳定跟住目标。
```

两者是上下游关系，不必重复做同一个 YOLO 检测赛道。

## 5. Active Tracking 中具体优化什么

MOT 输出的是：

```text
track_id, bbox, confidence
```

但云台真正需要的是：

```text
当前该跟谁？
目标相对画面中心偏了多少？
目标下一小段时间会在哪里？
这个偏差是否值得下发给云台？
Az 和 El 分别该转多少？
什么时候认为已经稳定？
目标短时丢失时是否继续预测？
```

所以 Active Tracking 的优化对象是：

> MOT 输出到云台控制之间的主动跟踪链路。

可以拆成五个具体模块。

### 5.1 主目标选择

多目标跟踪会输出多个目标 ID，但云台一次只能主要跟一个目标。

需要解决：

- 当前 `active_track_id` 怎么选。
- 目标短时遮挡时是否保持原目标。
- 新目标置信度更高时是否切换。
- 目标交叉时如何避免云台跳变。
- 如何减少频繁切换带来的云台摆动。

### 5.2 目标状态估计

MOT 给的是 bbox，但云台控制需要连续、平滑、可预测的目标状态。

需要完成：

```text
bbox center
-> 图像归一化偏差
-> 角度偏差
-> 速度估计
-> 预测位置
```

可优化点包括：

- Kalman 状态设计。
- 自适应 dt，根据实际帧率更新。
- 根据检测置信度调整观测可信度。
- 漏检时继续预测。
- 恢复检测时避免控制突变。

### 5.3 延迟预测补偿

真实系统存在多种延迟：

```text
摄像头曝光延迟
+ 图像传输延迟
+ YOLO 推理延迟
+ MOT 处理延迟
+ UDP/线程调度延迟
+ 串口指令延迟
+ 云台机械响应延迟
```

如果直接用当前检测框控制云台，本质是在追过去的目标位置。

基本思路是：

```text
predicted_x = x + vx * delay
predicted_y = y + vy * delay
```

进一步可研究：

- Az 轴和 El 轴使用不同预测延迟。
- 捕获阶段和稳定阶段使用不同策略。
- 高速目标和低速目标使用不同补偿权重。
- 端到端延迟和云台机械响应延迟分开建模。

### 5.4 云台控制决策

MOT 每帧都可能输出目标位置，但云台不能被每一帧微小抖动打断。

需要解决：

- 什么时候下发新命令。
- 什么时候忽略小误差。
- 什么时候只更新 Az。
- 什么时候只更新 El。
- 什么时候整条命令替换。
- 目标切换时如何避免“新方位 + 旧俯仰”的混合指向。

当前项目已有基础：

- 分轴抢占阈值。
- 同一 `track_id` 下按轴更新。
- `track_id` 切换时整条命令替换。
- 到位迟滞和稳定判定。
- 单一云台控制线程统一下发命令。

这些都可以转化为主动跟踪控制策略。

### 5.5 稳定判定与任务触发

MOT 不知道什么时候“跟住了”，但云台系统需要判断：

- 目标是否进入中心区域。
- 是否稳定足够久。
- 是否可以触发激光测距。
- 是否可以向 UI 回传有效状态。
- 目标丢失后是否禁止测距。

激光不一定是论文核心，但可以作为应用验证指标：

```text
稳定跟踪后，激光有效测距率是否提高。
```

## 6. 可能的研究主线

推荐主线：

> 面向嵌入式云台平台的多目标主动跟踪与延迟补偿控制方法。

更具体的表述：

> 本文不改变检测器本身，而是针对多目标跟踪结果到云台执行之间的主动跟踪链路进行优化，包括主目标保持、目标状态预测、延迟补偿和分轴控制决策，使云台能够在嵌入式实时条件下稳定保持目标居中。

可拆成三个贡献点：

1. 基于 MOT 输出的主目标状态估计方法。
2. 考虑推理延迟和云台响应延迟的预测补偿策略。
3. 面向 Pan/Pitch 动态差异的分轴控制决策与真实云台闭环验证。

## 7. 论文意义

这个方向的意义不在于再提出一个 YOLO 模块，而在于解决：

> 传统检测/多目标跟踪算法输出与真实云台闭环控制需求之间的不匹配。

普通 MOT 关注：

```text
MOTA、IDF1、HOTA、ID switch、轨迹丢失率。
```

云台主动跟踪更关注：

```text
目标中心误差、稳定时间、重捕获时间、云台指令抖动、目标保持率、激光有效触发率。
```

因此，同样的检测和 MOT 输出下，如果主动跟踪策略更好，就应该表现为：

- 目标更快回到画面中心。
- 中心误差更小。
- 丢失率更低。
- 云台指令更平滑。
- 多目标切换更稳定。
- 激光测距触发更可靠。

这体现的是主动感知系统的工程和方法价值。

## 8. 比较实验设计

### 8.1 固定 Detection + MOT，比较 Active Tracking 策略

固定检测器和跟踪器，例如：

```text
YOLO + ByteTrack
或
YOLO + 当前 Kalman-Hungarian tracker
```

对比主动跟踪策略：

```text
1. 直接用目标中心偏差控制云台
2. Kalman 平滑后控制
3. 统一延迟预测控制
4. 分轴延迟预测控制
5. 分轴延迟预测 + 主目标保持 + 抢占迟滞
```

指标：

- 平均中心误差。
- 最大中心误差。
- 目标进入中心区域所需时间。
- 目标丢失率。
- 重捕获时间。
- 云台指令下发次数。
- 指令覆盖/抢占次数。
- 激光有效测距率。

### 8.2 固定 Active Tracking，比较 MOT 基线

检测器固定，主动控制策略固定，对比：

```text
SORT
DeepSORT
ByteTrack
OC-SORT
BoT-SORT
当前项目 tracker
```

指标包括传统 MOT 指标和云台闭环指标。

重点观察：

```text
MOT 指标更好，是否一定导致云台跟踪更稳定？
```

这可以体现主动跟踪场景和普通 MOT 场景的区别。

### 8.3 部署与实时性实验

在 RK3588 上测试：

- 检测 FPS。
- 跟踪耗时。
- 控制线程耗时。
- 端到端延迟。
- NPU/CPU 占用。
- 不同输入分辨率下的精度和延迟。

这些实验不一定是主创新，但能支撑嵌入式场景的完整性。

## 9. 建议优先阅读的论文线索

### MOT 基础线

- SORT
- DeepSORT
- ByteTrack
- OC-SORT
- BoT-SORT
- StrongSORT

重点看：

- 检测框如何关联成轨迹。
- Kalman 状态如何设计。
- 匈牙利匹配代价如何构造。
- 遮挡和漏检如何处理。
- ID switch 如何减少。

### 小目标检测与嵌入式部署线

- UAV 小目标检测综述。
- 小目标检测与跟踪综述。
- YOLOv8/YOLO11/RT-DETR lightweight。
- K230、RK3588、Jetson 等边缘部署论文。
- 师兄 DNW-YOLO 论文。

重点看：

- 小目标为什么难检测。
- 精度、模型大小、FPS、功耗如何权衡。
- 板端指标如何报告。

### Active Tracking / Gimbal Tracking 线

重点看：

- 主动视觉跟踪。
- PTZ/gimbal camera tracking。
- 视觉伺服控制。
- 云台目标居中控制。
- 延迟补偿控制。

读论文时要特别关注：

- 是否有真实硬件闭环。
- 是否考虑推理延迟。
- 是否考虑云台机械响应。
- 是否区分 Pan/Pitch 动态差异。
- 评价指标是否包含中心误差和稳定时间。

## 10. 当前建议

短期不要急着定死创新点。建议先把研究问题写成：

```text
问题 1：普通 MOT 输出能否直接用于云台闭环控制？
问题 2：推理延迟和云台响应延迟如何影响目标中心误差？
问题 3：Pan/Pitch 分轴动态差异是否需要不同预测补偿？
问题 4：多目标场景下，主目标保持和目标切换如何影响云台稳定性？
```

后续读完一轮论文后，再收敛成一个具体题目。

目前最稳的方向判断是：

> 不重复师兄的 Detection 论文路线，而是基于检测结果，研究嵌入式云台平台上的多目标主动跟踪闭环。

